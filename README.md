# J.A.R.V.I.S — Just A Rather Very Intelligent System

> עוזר AI ריבוני לחלוטין. **אפס API keys. אפס ענן. אפס משקולות שהורדו מבחוץ.**
> הטוקנייזר, המודל העצבי, מנוע הדיבור, זיהוי הקול, הזיכרון, סוכני המשנה, ה־HUD והשליטה במחשב —
> **הכול נבנה ואומן כאן, מאפס.**

📖 **[תוכנית האב המלאה → `docs/PLAN.md`](docs/PLAN.md)**

---

## מה זה

אפליקציית Desktop מלאה (Electron, frameless, מסך מלא) עם מוח AI היברידי ש:

- 🧠 **חושב** — Transformer שנבנה ואומן מאפס על קורפוס שייצרנו בעצמנו + קרנל סימבולי (תכנון, כלים, אימות, מניעת הזיות)
- 🗣️ **מדבר ומקשיב** — TTS קונקטיב בקול אמיתי + זיהוי פקודות קוליות ומילת השכמה, הכול מקומי
- 🎙️ **שיחה חופשית** — לחיצה על 🗣 פותחת מיקרופון **שכבר מקשיב**: פשוט מדברים, הוא עונה בקול וממשיך להקשיב. בלי לחיצות, עם זיהוי סיום משפט (VAD), השתקה עצמית בזמן שהוא מדבר, ושמירה על האזנה גם בחדר שקט
- 🎯 **אימון קול** — הבנק המסופק מזהה את הקול המסונתז שלנו; קול אנושי אחר חייב דגימות משלו. לוחצים 🎯 אימון קול, מקריאים 6 משפטים, וזהו. וכשהוא לא מבין — הוא מחזיר את ההקלטה וכפתור אחד מלמד אותו
- 💬 **צ'אט שנשאר** — היסטוריית השיחה נכתבת ל־`data/transcript.jsonl` ונטענת חזרה, כך שאתחול לא מוחק את השיחה
- 💻 **מתכנת** — `HEPHAESTUS` כותב קוד, **מריץ אותו בארגז חול, קורא את השגיאה ומתקן את עצמו**
- 🖥️ **שולט במחשב** — קבצים, תהליכים, אפליקציות, קליפבורד, צילום מסך, מדיה, אוטומציה (Windows)
- 🧬 **זוכר** — זיכרון אפיזודי, סמנטי ופרוצדורלי עם שכחה חכמה, נשמר ב־SQLite
- 🔒 **מפוקח** — חומת הרשאות, אישור אנושי לפעולות CRITICAL, יומן ביקורת, dry-run, מתג חירום
- ✨ **נראה כמו מחר** — Arc Reactor חי על canvas, זרם אירועים בזמן אמת, טלמטריה, גלים קוליים סביב הליבה

## סוכנים

| סוכן | תפקיד | קוד |
|---|---|---|
| **JARVIS** | המנצח — שיחה, כוונה, תכנון, החלטות, קול | `agents/jarvis.py` |
| **HEPHAESTUS** | המתכנת — כתיבה, הרצה, תיקון עצמי, בדיקות | `agents/hephaestus.py` |
| **MNEMOSYNE** | הזיכרון — שינון ושליפה | `brain/memory.py` |
| **ARGUS** | העיניים — טלמטריה, מסך, אבטחה | `skills/sk_system.py`, `security/` |
| **HERMES** | הידיים — ביצוע פעולות במערכת | `skills/` |

## ארכיטקטורה

```
  ┌────────────────────── Electron (desktop shell) ─────────────────────┐
  │  ui/ · hud.js (canvas) · app.js (WS/REST + מיקרופון חי) · audio.js  │
  │  frameless HUD · global hotkeys · tray · spawns & supervises ↓      │
  └───────────────┬────────────────────────────────────┬────────────────┘
                  │ WebSocket /ws                      │ mic WAV → POST /api/listen
                  │ (events · commands · audio frames) │
  ┌───────────────▼────────────────────────────────────▼────────────────┐
  │              core/server.py — aiohttp, 127.0.0.1 only               │
  │   never blocks: every command runs as a task so a CRITICAL prompt    │
  │   can be answered on the same socket that is waiting for it          │
  └───────────────┬─────────────────────────────────────────────────────┘
                  │
        agents/jarvis.py  (orchestrator: boot · turns · voice · controls)
                  │
   ┌──────────────┼───────────────┬──────────────┬────────────────┐
   ▼              ▼               ▼              ▼                ▼
brain/         skills/        security/       voice/           core/bus.py
reasoning      54 skills      firewall        tts (concat)     every event,
+ neural core  + registry     + audit         stt (MFCC/DTW)   ring-buffered
+ knowledge    + validation   + kill switch   + dsp            = observability
+ memory
+ intent
```

**עיקרון מנחה:** הרשת העצבית מנסחת ומנתבת; **שום מספר ושום פעולה לא עוברים בלי קרנל סימבולי.**
מתמטיקה מחושבת במנוע מתמטי, עובדות נשלפות ממאגר הידע, קוד נבדק על ידי הרצה אמיתית,
ופעולות במערכת עוברות רק דרך חומת ההרשאות. מה שלא ודאי — נאמר כלא־ודאי.

### ארבעה שומרי כנות (כולם נולדו מבאגים אמיתיים שמצאנו)

| השומר | הבעיה שהוא פותר |
|---|---|
| **רלוונטיות נושאית** (`shares_topic`) | דמיון n-gram של תווים נפל על קידומת משותפת: *"ספר לי על חורים שחורים"* התאים ל*"ספר לי בדיחה"* (0.575) והמערכת הגישה בדיחה כ**עובדה מאומתת**. עכשיו תשובה מקורקעת חייבת לחלוק מילה נושאית אמיתית עם השאלה |
| **דגל `understood`** בסוכן המתכנת | כשלא נמצאה תבנית משימה, HEPHAESTUS בנה שלד ריק, העביר בדיקת עשן שלא יכולה להיכשל, ודיווח *"כתבתי, הרצתי ובדקתי — הכול עובר"*. עכשיו הוא אומר במפורש שזה שלד בלבד, והמתזמר מחפש תשובה אמיתית במקום אחר |
| **היגיינת זיכרון פרוצדורלי** | שלדים שנשמרו כ"מיומנות" שוחזרו אחר כך כפתרונות ידועים. שלד לעולם לא נכנס לזיכרון, ושורות הזבל הישנות נוקו |
| **מסך אי-רצף** (non-sequitur) | על שאלות מחוץ לתחום הליבה הקטנה פלטה סלט מילים מתוך הקורפוס (*"מי כתב את ההמלט"* → *"אני מריץ את הקוד עכשיו"*). תשובה שלא חולקת אף מילה נושאית עם השאלה נזרקת, ובמקומה באה ההודאה הכנה: *"אין לי תשובה ודאית לזה, אדוני, ואני לא ממציא"* |

`grounded=true` מוצג ב־HUD רק כשיש מקור דטרמיניסטי באמת: מנוע המתמטיקה, רשומת מאגר ידע,
תוצאת כלי, או תשובת זהות/נימוס שכתבנו בעצמנו. ניסוח חופשי של הרשת — תמיד מסומן ככזה.


---

## התחלה מהירה (Windows)

```bat
:: פעם אחת — מתקין Python deps + Electron
tools\setup_windows.bat

:: הפעלה — HUD שולחן עבודה (Electron ירים את המוח בעצמו)
JARVIS.bat
```

מצב פיתוח / דפדפן (בלי Electron):

```bash
pip install -r requirements.txt
python -m core.server            # http://127.0.0.1:8756
                                 #   WebSocket /ws + REST /api/command (תאום)
                                 #   ה־UI נופל אוטומטית ל־REST אם פרוקסי מסרב
                                 #   לשדרג את החיבור — בלי לאבד שמע או שליטה
python run_jarvis.py --browser   # אוטומטית: שרת + דפדפן
```

בדיקות (הכול רץ מקומית, בלי רשת):

```bash
python tests/run_all.py          # כל הסוויטה
python tests/run_all.py --quick  # בלי אינטגרציה כבדה
```

קיצורי מקש: **`F5` שיחה מיידית** (מקדים את החלון, ממקד את שורת הצ׳אט ופותח את
המיקרופון — אפשר לדבר בלי לקום מהכיסא; פועל גם כשחלון אחר בפוקוס) ·
`Ctrl+Shift+V` **שיחה חופשית** · `Ctrl+Shift+Space` דיבור בלחיצה ·
`Ctrl+Shift+J` נעיצה למעלה · `Ctrl+Shift+Esc` מתג חירום.

**בחירת מיקרופון ומצלמה:** בשורת הקיצורים יש בורר `🎚` — הוא מפרט כל מכשיר קלט
שהמערכת מדווחת עליו, שומר את הבחירה בין הפעלות, וחוזר לברירת המחדל אם המכשיר
נותק. הבורר תקף לדיבור, לשיחה החופשית, לאימון הקול ולזיהוי הפנים יחד.

### 👁 פעם אחת: ללמד אותו מי אתה (20 שניות)

JARVIS יודע **מי** יושב מולו, ונותן לאדם הזה — ולא לאף אחד אחר — את ההרשאות שלו.
הכול מאפס: אין OpenCV, אין Haar, אין משקולות שהורדו, אין ענן.

1. פאנל **ראייה · מי מולי** → **📷 מצלמה**.
2. שם + רמת הרשאה (`SAFE` צפייה / `WRITE` כתיבה / `CRITICAL` שליטה) → **רשום**.
   כדאי 3–4 דגימות בתאורה ובמרחק שונים; כל דגימה מחליפה את הקודמת בגלריה.
3. מרגע זה הוא מדווח `✓ אדון · CRITICAL · 100%` — וחומת ההרשאות באמת עוברת לרמה הזו.

**הפנים הראשונות הן של הבעלים.** מי שנרשם ראשון לגלריה ריקה הופך אוטומטית לבעלים:
`role=owner` ורמת `CRITICAL` — גם אם ביקשת `SAFE`, כי לבקש `SAFE` על המכונה שלך היה
נועל אותך מחוץ לעוזר שלך. הכלל **מיקומי ולא שמי**: שם אפשר להקליד כל מי שעומד מול
המצלמה, אבל "היית כאן כשהגלריה הייתה ריקה" אי אפשר לטעון בדיעבד. `CRITICAL` הוא של
הבעלים בלבד — אורח שנרשם ב‑`CRITICAL` מוגבל ל‑`WRITE`, ויומן האירועים מציין זאת.
`set_level` מסרב להוריד את הבעלים אלא אם כן עוברים דרך `force=True`, כדי שלחיצה
מוטעית ב‑HUD או אורח שמצא את נקודת הניהול לא ישללו אותה. שם הבעלים הצפוי הוא
`OSCAR`, והגלריה מגיעה ריקה — ההרשמה הראשונה שלך תופסת את המקום.

מהטרמינל: `python tools/enroll_face.py --image your_face.png`. הכלי קורא PNG דרך
המפענח שלנו (בלי ספריית תמונה חיצונית), מדפיס את תיאור הסצנה של מה שקיבל לפני
שהוא רושם, אומר בקול כשהרשמה היא זו שתתפוס את מקום הבעלים, ומסרב למחוק בעלים בלי
`--force`. גם `--list` להצגת הגלריה ו‑`--owner <id>` להעברת בעלות.

**איך זה עובד.** עור מזוהה ב־YCbCr (עם פסילת אפור נייטרלי — בלעדיו שיער אפור
ומלט נחשבים עור), מנקה מורפולוגית שסוגרת חורים, ואז מסננת גיאומטריה + בדיקת
מבנה: לפנים יש שקעי עיניים כהים, לקיר בצבע עור אין. הזיהוי הוא eigenfaces —
הפנים מיושרים לפי מיקום העיניים (סיבוב/קנה מידה/הזזה מנוטרלים), מנורמלים
ל־64×64, ומושווים לגלריה. הסף **לא מנוחש אלא נמדד**: 57 פריימים שלא נרשמו מעולם,
בגלריות של 1/2/3/5/8 אנשים — כל הרשומים התקבלו (0.915–1.000) וכל הזרים נדחו
(≤0.789); הסף 0.85 יושב בדיוק בפער הזה.

**ההרשאה קשורה לנוכחות.** זה החלק החשוב: הזיהוי מעניק רמה ל־12 שניות בלבד. מי
שיוצא מהפריים — הרמה חוזרת לברירת המחדל, גם בלי פריים חדש. זר מול המצלמה מקבל
`SAFE`, והתקרה המוגדרת לעולם לא נפרצת (אדם עם `CRITICAL` תחת תקרת `WRITE` יקבל
`WRITE`). יש גם debounce של שני פריימים, כדי שהבהוב לא יחליף הרשאות.
כיבוי: הסר את הסימון מ־**הרשאה לפי פנים** — הזיהוי ממשיך לדווח, רק לא נוגע
בחומת ההשקט.

**מה הוא רואה, לא רק מי מולו.** `vision/scene.py` מודד מהפיקסלים בלבד — בלי מודל
ובלי רשת — את מה שמסביב לשאלת הזיהוי: פסיקת תאורה (`good/dim/dark/blown/flat`) עם
בדיקת תאורה אחורית אמיתית שמשווה את הבהירות בפנים לזו שסביבן, כי חלון מאחורי
האדם הורס זיהוי בזמן שהפריים נראה לכם תקין; כיוון ראש ומרחק מהגיאומטריה שכבר
חושבה; הפרש פריימים לתנועה; ציון איכות שעונה על השאלה היחידה שמשנה — האם הפריים
הזה יכול היה לתמוך בזיהוי; ומשפט עברי שה‑HUD מציג כלשונו, כדי שהספים יחיו במקום
אחד. הפאנל מציג את המשפט, את האזהרות שאפשר לפעול לפיהן («חשוך מדי לזיהוי»),
וסרגלי איכות ותאורה.

**חיוּת — עם הגבולות בחוץ, לא קבורים.** המודד מזהה **מסך** באמינות: פנים אמיתיות
נמדדות במוארה 3.83–7.45 לעומת 32.4–105.0 לרשת פיקסלים של תצוגה, והסף 12.0 יושב
בתוך הפער וקרוב יותר לפנים. הוא **לא מזהה תצלום מודפס בכלל**. מדד רביעי שנועד
לתפוס דף מוחזק מול העדשה נבנה, נמדד, ונמחק: פנים אמיתיות קיבלו בו 0.146–0.740
והדף 0.724, כי תיבת הזיהוי צמודה לכתם העור והטבעת סביבה תופסת את מתאר הפנים עצמן
כקו ישר ארוך. הוא דלק על פנים אמיתיות ולא הפריד כלום, אז הוא מחוק ולא מכוון עד
שייראה משכנע. חיוּת יכולה רק **לחסור** — היא לעולם לא מעלה הרשאה, ופריים חשוד
נכפה ל‑`SAFE`.

נקודות הקצה: `GET /api/faces` (גלריה + מצב השער + הכיול) ·
`POST /api/faces/recognize` · `POST /api/faces/enroll` · `POST /api/faces/admin`
(`remove|set_level|arm|disarm|poll|configure`). הפריימים נשלחים כ־RGB גולמי
מקנבס, אז אין שום מקודד תמונה בשרשרת. הגלריה נשמרת ב־`data/faces/gallery.json`.

### 🎯 פעם אחת: ללמד אותו את הקול שלך (30 שניות)

הבנק המסופק נבנה מהקול המסונתז של JARVIS, ולכן **קול אנושי אחר לא יזוהה ממנו** (נמדד: 0.000).
האימון הוא מה שהופך את הדיבור לעובד, לא תוספת נחמדה:

1. ב־HUD לוחצים **🗣 שיחה** → המיקרופון נפתח *כבר מקשיב* (אין צורך במילת השכמה).
2. לוחצים **🎯 אימון קול** → מקריאים את 6 המשפטים; כל הקלטה נעצרת לבד אחרי 3 שניות.
   או **🎯 אמן הכול ברצף** ואז רק מדברים.
3. כל דגימה מקבלת ציון מיד: `✓ 100% — הוא מכיר את הקול שלך`.
4. אפשר גם בלי HUD: `python tools/enroll_voice.py --record` (ואז `--verify`).

שני דברים שעוזרים לאבחן בלי לנחש:
- **`זיהוי קול 0.12/0.55`** בשורת השיחה — הציון החי של מילת ההשכמה. נמוך? תעשה אימון.
  אפס מוחלט ולאורך זמן? המיקרופון עצמו לא שולח קול (וזה ייאמר במפורש).
- **`🎯 למד אותי והפעל`** — כשהוא לא מבין, הוא מחזיר את ההקלטה עצמה; בוחרים מה אמרתם,
  והוא מאמן את הדגימה *ומריץ אותה מיד*, כך שהתיקון וההוכחה מגיעים יחד.

נקודות הקצה: `GET /api/enroll` (רשימת האימון + אוצר המילים) · `POST /api/enroll?label=wake`
(גוף = WAV) · `POST /api/voice {"action":"unmute"}` (פתיחת המיקרופון ברגע שההשמעה באמת נגמרה).
הבנק האישי ניתן להפרדה מקוד המקור: `JARVIS_STT_BANK=~/jarvis-voice`.

---

## מה באמת עובד — נמדד, לא מוצהר

| רכיב | מצב | מספרים |
|---|---|---|
| טוקנייזר BPE | ✅ מאפס | 8,057 טוקנים · 7,791 מיזוגים · 2.999 תו/טוקן · 0 כשלי שחזור ב־2,000 דגימות |
| קורפוס | ✅ מיוצר עצמית | 100,910 דוגמאות אימון / 5,312 בדיקה · סדר ReAct נכון (user→think→plan→tool→answer) |
| ליבה עצבית | ✅ אומנה כאן | NANO · 6.20M פרמטרים · 6 שכבות · dev loss 0.188 · **perplexity 1.207** · 37 דקות אימון על 2 ליבות CPU |
| מנוע מתמטיקה | ✅ | ארבע פעולות, חזקות, שורשים, לוגריתמים, טריגונומטריה, עצרת, פיבונאצ'י, ראשוניות, בסיסים, סטטיסטיקה, משוואות — מדויק לחלוטין |
| מנתב כוונות | ✅ | 17 כוונות · מתמטיקה/קוד/קבצים/אפליקציות/זיכרון/מערכת/RAG/ראייה · חילוץ נתיב שומר סיומת (`docs/PLAN.md`) ומזהה סוג קובץ מדובר ("קבצי פייתון" ← `*.py`) |
| מאגר ידע | ✅ | 53 רשומות · 128 זוגות שאלה־תשובה · 16 נושאים · אחזור char-n-gram TF-IDF מאפס + שער רלוונטיות נושאית |
| כלים (skills) | ✅ | 71 כלים · SAFE=55 / WRITE=9 / CRITICAL=7 · 11 מודולים |
| נוכחות (פנים → מוח) | ✅ | `brain/presence.py` מגשר בין המצלמה להסקה: המוח יודע מי מולו, מברך בשם, ומסביר חסימה במונחים של מי שנמצא בחדר · TTL זהה ל‑`FaceGate` (12 שניות) כדי שלא ייווצר פער · הראייה רק *מגרעת* מהרשאה, לעולם לא מעלה |
| HEPHAESTUS | ✅ | 10 תבניות משימה · ארגז חול מבודד · 8 חוקי תיקון עצמי · מחולל בדיקות מודע-טיפוסים · זיכרון פרוצדורלי |
| TTS (דיבור) | ✅ מאפס | מנוע קונקטיבציה: 118 מילים + 381 פונמות שהוקלטו בקול אמיתי · כיסוי 26/29 פונמות · 24kHz · RTF 0.26–0.56 · **קצב דיבור טבעי 9–12 תווים/שנייה** (תוקן מ־4.2: תשובה של 91 תווים ירדה מ־21.5s ל־10.0s) |
| STT (זיהוי) | ✅ מאפס | 41 פקודות + מילת השכמה · 984 תבניות (הרחבת גיוון: ±3 חצאי טון, 2 קצבים, רעש 20dB) · בדיקה עצמית **82/82** ביטויים זוהו · **314ms לביטוי** · כיול: אותו־ביטוי 5.71 מול ביטוי־אחר 6.87 (הפרדה 1.2x) · **קול אנושי אחר דורש אימון — ראו "הגינות"** |
| ראייה / זיהוי פנים | ✅ מאפס | גילוי עור ב־YCbCr + מבנה עיניים · יישור לפי העיניים (סיבוב/מרחק/הזזה) · eigenfaces 64×64 · **נמדד: 57/57 פריימים שלא נרשמו זוהו נכון, 0/50 זרים התקבלו, סף 0.85 בתוך הפער 0.789–0.915** · גלריה נשמרת כ־JSON · בלי OpenCV/ענן |
| הרשאה לפי נוכחות | ✅ מאפס | זהות → רמת הרשאה בחומת ההשקט · **חכירה של 12s**: יציאה מהפריים שוללת את ההרשאה גם בלי פריים חדש · זר = `SAFE` · תקרה שלא נפרצת · debounce של 2 פריימים · **הפנים הראשונות = בעלים ב‑`CRITICAL`, ו‑`CRITICAL` הוא של הבעלים בלבד** (אורח שנרשם בו מוגבל ל‑`WRITE`) · נשלט ב־HUD |
| הבנת סצנה | ✅ מאפס | `vision/scene.py` · פסיקת תאורה + בדיקת תאורה אחורית אמיתית · כיוון ראש ומרחק · תנועה מהפרש פריימים · ציון איכות פריים · משפט עברי ל‑HUD · **חיוּת: פנים אמיתיות מוארה 3.83–7.45 לעומת 32.4–105.0 לתצוגה, סף 12.0 בתוך הפער, 8/8 פנים בגלריית הבדיקות לא סומנו (ו‑12/12 כולל 4 רמות תאורה)** · מזהה מסך, **לא** תצלום מודפס |
| חומת הרשאות | ✅ | רמות SAFE/WRITE/CRITICAL · אישור אנושי בזמן אמת · יומן ביקורת JSONL · dry-run · מתג חירום |
| HUD | ✅ | Electron frameless · Arc Reactor על canvas · זרם אירועים חי · טלמטריה · תורי הרשאות · שמע דרך WebSocket |
| שרת מקומי | ✅ | aiohttp · 127.0.0.1 בלבד · WebSocket + REST/`api/command` תאום על מנתב אחד · 9 נקודות REST · CORS ל־renderer · אפס תלות רשת |
| שיחה חופשית | ✅ מאפס | הזרמת מיקרופון חיה (WS בינארי + REST תאום) · נפתחת ישירות להאזנה (בלי תלות במילת השכמה) · VAD לסיום משפט · שומר זנב־השכמה (משפט של 1.0s הגיע כ־2.76s וזוהה ב־0.29 — תוקן, כעת 1.60s וביטחון 1.00) · השתקה עצמית ש**מדווחת** על עצמה ונפתחת כשההשמעה באמת מסתיימת · האזנה מחדש בחדר שקט · מדד "זיהוי קול" חי ב־HUD |
| אימון קול | ✅ מאפס | `GET/POST /api/enroll` · לוח אימון ב־HUD (6 משפטים, הקלטה נעצרת לבד) · **נמדד:** קול זר על מילת ההשכמה 0.000 לפני → 1.000 אחרי שתי דגימות, על ביטויים שלא נשמרו · teach-back: משפט שלא זוהה חוזר כ־WAV וכפתור אחד מאמן ומפעיל אותו מיד · `JARVIS_STT_BANK` להפרדת הבנק האישי מקוד המקור |
| היסטוריית צ'אט | ✅ | נשמרת ל־`data/transcript.jsonl` · נטענת באתחול · נמחקת בפקודת `clear_chat` |
| קלט נגיש | ✅ | `F5` מקדים חלון + ממקד צ׳אט + פותח מיקרופון (גם כשחלון אחר בפוקוס) · בורר מיקרופון ומצלמה עם שמירת בחייה ונסיגה לברירת מחדל בניתוק |
| עברית בווינדוס | ✅ תוקן | `chcp 65001` + `PYTHONUTF8`/`PYTHONIOENCODING` בכל ההרצות, וכל לכידת subprocess עם `errors="replace"` — קודם ילד שכתב cp1255 הפיל את ה־self-test כולו |
| **RAG על קבצים מקומיים** | ✅ מאפס | קריאה + אינדוקס + שליפה + תשובה מעוגנת בציטוט · **74 קבצים / 1,521 קטעים / 10,311 מונחים / 1.5MB ב‑3.6 שניות** · שליפה חציון **119ms** (p95 257ms) · שאילתה מלאה **173ms** חציון · אינדוקס אינקרמנטלי **0.03s** (74/74 ללא שינוי) · **17/17 ציטוטים אומתו מילולית מול הקובץ בדיסק** · BM25 + char-n-gram + phrase + MMR · SQLite + inverted index · אפס embeddings חיצוניים |
| בדיקות | ✅ | **16 מודולים · 1,070 בדיקות · 0 כשלים** (~278 שניות) |

### פירוט הבדיקות

```
tests/test_brain_smoke.py      טוקנייזר, מודל, KV-cache, ייצוא/טעינה
tests/test_intent_math.py      מנוע מתמטיקה + ניתוב כוונות + קרקוע + שומרי כנות
tests/test_skills_security.py  מרשם הכלים, אימות ארגומנטים, חומת הרשאות, ביקורת
tests/test_coder.py            ארגז חול, 10 תבניות, תיקון עצמי, הסבר/ניתוח, כנות דיווק
                               (שלד ≠ פתרון) והיגיינת זיכרון פרוצדורלי
tests/test_orchestrator.py     אתחול כן (15 בדיקות אמיתיות), שיחה, קול, אישורים, בקרה
tests/test_stt.py              DTW מול brute-force, WAV, MFCC, בנק, דיוק, דחייה, השכמה
tests/test_server.py           HTTP + WebSocket אמיתיים מול שרת חי, לולאת קול מלאה,
                               תעבורת ה־REST התאומה + זיקת חומת ההרשאות בשתי התעבורות
tests/test_vision.py           פנים סינתטיים (זהות = פרופורציות/גוון/שיער, רעש = פריים):
                               גילוי, דחיית קיר/רעש/פאנל שטוח, שני אנשים בפריים, יישור
                               לפי עיניים, גלריה ושמירה, זיהוי מפריימים שלא נרשמו, דחיית
                               זרים, שער נוכחות ותפוגת חכירה, תקרה, כיול הסף על 5 גדלי
                               גלריה, ותעבורת הפריים
tests/test_scene.py            הבנת סצנה ושכבת הבעלים: פסיקת תאורה כולל תאורה אחורית,
                               כיוון ראש ומרחק, תנועה, ציון איכות פריים, תקציר עברי,
                               **חיוּת — ומה שהיא לא מוכיחה** (הארטיפקטים בנויים, לא
                               מצולמים; הבדיקות מוכיחות שהמדד מפריד תמונה רחבת־סרט
                               ממחזורית ושפנים אמיתיות לעולם אינן אלה שמסומנות),
                               וכלל הבעלים: הראשון נרשם כ־owner ב‑CRITICAL גם כשביקשו
                               SAFE, אורח ב‑CRITICAL מוגבל ל‑WRITE, set_level מסרב
                               להוריד בעלים, העברת בעלות משאירה בדיוק בעלים אחד,
                               והתפקיד שורד טעינה מחדש
tests/test_presence.py         **פנים שמגיעות למוח**: חדר ריק לא ממציא אדם (ואין ברכה),
                               בעלים מוצג כבעלים, אורח נחסם מ‑CRITICAL *עם הסבר בעברית*,
                               חיוּת חשודה מורידה ל‑SAFE גם לבעלים, חומת אש ב‑SAFE
                                 מגבילה זהות CRITICAL, תפוגה מחזירה "לא ידוע" במקום
                               "מישהו כאן", ארבעת סקילי הראייה רשומים וב‑SAFE בלבד,
                               ניתוב שאלות החדר לכלי הנכון, `presence` מופיע ב‑trace,
                               **ושהבודק באמת פועל**: מספר שלא ניתן לאיתור מוריד
                               קרקוע וביטחון במקום לצאת כאמת
tests/test_platform.py         הקידוד בווינדוס: ילד שכותב cp1255/cp862 לא מפיל את ההורה,
                               ה־RESULT בכל זאת מגיע, run_all מאולץ ל־UTF-8, ה־.bat
                               מעבירים codepage, וכל לכידת text=True מכוסה
tests/test_rag.py              RAG על קבצים מקומיים: קידוד (cp1255 לעומת UTF‑8 עברי, וכלל
                               זיהוי־בינארי שהיה דוחה כל קובץ עברי), דחיית מפתחות פרטיים
                               ו־.env לפני קריאה, שלמות טווחי שורות מול שורות הקובץ האמיתיות,
                               חפיפה מוגבלת לתקציב, פוסטינגים/אינדוקס אינקרמנטלי/גיזום,
                               **אימות הקרקוע מול בייטים אמיתיים בדיסק** (כולל וידוא שהבודק
                               באמת נכשל כשהקובץ לא מכיל את הציטוט), סירוב כן במקום ציטוט
                               שנשמע סביר, פיזור ביטחון שאינו רווי, היגיינת ציטוטים,
                               proportional phrase evidence, סיכוני הרשאות, ניתוב שלא חוטף
                               כוונות שכנות, וחסימת אינדוקס ברמת SAFE דרך חומת ההרשאות
tests/test_voice_session.py    שיחה חופשית: ה־TTS שלנו מזין את ה־STT שלנו דרך שרת חי —
                               השכמה, סיום משפט, תשובה בקול, השתקה עצמית, פקודת עצור,
                               timeout, WS בינארי, REST תאום, היסטוריית צ'אט, פתיחה ישירה
                               להאזנה, האזנה מחדש בשקט, teach-back, ואימון קול של קול זר
                               מול בנק זמני (הבנק המסופק לא ננגע)
```

---

## RAG על הקבצים המקומיים שלך

שלוש פעולות, ולא יותר: **קריאה → אינדוקס → תשובה עם מקור**. בלי שירות embeddings,
בלי מפתח API, בלי הורדה. האינדקס הוא קובץ SQLite אחד ב‑`data/` (ב‑gitignore) —
מחיקה שלו עולה ב־rebuild, לא במסמכים שלך.

```bash
# לבנות את האינדקס
python tools/build_rag_index.py --root "C:\Users\me\Documents"
python tools/build_rag_index.py --root ~/notes --root ~/projects --force

# לשאול ממנו, עם ציטוט מקור
python tools/build_rag_index.py --ask "מה כתוב על חומת ההרשאות"
python tools/build_rag_index.py --search "permission firewall" --k 5
python tools/build_rag_index.py --report
```

ב‑HUD יש פאנל «קבצים מקומיים · שליפה»: שדה תיקייה + כפתור אנדקס, שדה שאלה,
והתשובה מוצגת עם תג «מעוגן/חלש», אחוז הביטחון, ורשימת הציטוטים כשורות נפרדות.
אם שום דבר לא מאונדקס — הפאנל אומר את זה במקום להציג רשימה ריקה שנראית כמו
"אין התאמות". אפשר גם בקול/בצ'אט: «חפש בקבצים שלי איך מריצים בדיקות»,
«מה כתוב בקבצים על חומת הרשאות», «תאנדקס את התיקייה docs», «מה מצב האינדקס».

**למה התשובה extractive ולא paraphrase.** המודל העצבי שלנו טוב במיון וניווט, לא
בכתיבת עברית חופשית. אם ניתן לו לנסח, הציטוטים יהיו קישוט — הוא יגיד משהו שאף
מקור לא אומר ולא תהיה דרך לגלות את זה. לכן כל משפט בגוף התשובה **מועתק מילולית**
מתוך קטע מאונדקס, ו‑`verify_answer()` קורא מחדש את הקובץ מהדיסק ובודק שהמשפט באמת
שם. ה‑API מפרסם את הפסיקה הזו (`verified`) — לקוח שרואה `false` יודע לא לסמוך.

**מדידה אמיתית** (הקורפוס: הריפו הזה, 74 קבצים, בלי `tests/` ובלי `brain/rag/` —
שני המקומות שמצטטים את שאלות המדידה עצמן, ולכן מדידה מולם היא מעגלית):

| מדד | תוצאה |
|---|---|
| אינדוקס | 74 קבצים · 1,521 קטעים · 10,311 מונחים · 1.5MB · **3.59s** |
| אינדוקס חוזר (אין שינוי) | **0.03s** · 74/74 זוהו כלא־השתנו · 0 נקראו מחדש |
| שליפה | חציון **119ms** · p95 **257ms** (n=51) |
| שאילתה מלאה (שליפה + ניסוח) | חציון **173ms** · מקסימום 272ms |
| 11 שאלות שהקורפוס עונה עליהן | 8 `grounded` · 3 `weak` · **0 `none`** |
| 6 שאלות שהקורפוס לא עונה עליהן | 3 `none` · 2 `weak` · **1 `grounded` (שגוי)** |
| אימות קרקוע | **17/17** הציטוטים נמצאו מילולית בקובץ המצוטט |
| פיזור ביטחון | 15 ערכים שונים מתוך 17 · ממוצע ענו 0.566 מול ממוצע סירוב 0.219 |

**ה‑false positive היחיד, בלי ייפוי:** «מה מספר הטלפון של הרופא שלי» חזר כ‑`grounded`
(0.597). הסיבה אמיתית ולא מעניינת פחות מהתוצאה: `tools/forge_corpus.py` מכיל את
המשפט «מה מספר הטלפון של השכן שלי?» כדוגמה לאימון סירוב — כלומר הקורפוס **באמת**
מכיל כמעט את השאלה הזו מילולית, והציטוט מאומת. זה כשל של קורפוס שמכיל דוגמאות
של עצמו, לא של המנוע. אין לי דרך כנה לתקן את זה בשכבה הלקסיקלית.

**גבולות כנים של השכבה הזו:**

- **אין סמנטיקה.** «GPU» ו‑«cuda» לא ייפגשו, «סקיל» ו‑«skill» לא ייפגשו. ניסיתי
  שלושה מנגנונים: stemming עברי קל, הרחבה מורפולוגית מהאוצר המאונדקס (prefix +
  suffix + trigram Jaccard), ו־char-n-gram. הם עוזרים לנטיות («מריצים»→«ריצ»←«הריץ»)
  ולא לתרגום. הפתרון האמיתי הוא embeddings, וזה דורש משקולות — מחוץ ל‑`offline_max`.
- **שער הכיסוי נבדק ונזרק.** הכלל «קטע חייב להכיל ≥60% ממונחי השאלה» נראה מובן
  מאליו ונמדד רע: שאלות שהקורפוס *כן* עונה עליהן ישבו בכיסוי 0.33–0.50, ושאלה
  לא־רלוונטית הגיעה ל‑1.00. הוא הרג 3 true positives ולא תפס כלום. הכיסוי נשאר
  כמשקל בציון, לא כשער.
- **OCR לא קיים**, ולכן PDF סרוק ותמונות לא נכנסים לאינדקס. טקסטואלי בלבד.
- **האינדוקס לא נבנה לבד.** `roots` ריק בברירת מחדל ו‑`auto_index_on_boot=False` —
  המערכת לא סורקת את הדיסק שלך עד שאתה אומר לה איזו תיקייה.

---

## הגינות: מה עדיין לא

- **גם בשיחה החופשית אוצר המילים נשאר סגור**: המיקרופון פתוח והתורות מתנהלים לבד, אבל מה שמזוהה
  הוא 25 הפקודות + מילת השכמה — לא הכתבה חופשית של כל משפט. משפט מחוץ לאוצר יזוהה כ"לא שמעתי"
  (וייאמר בכנות), לא יומצא.
- **שיחה חופשית דורשת הרשאת מיקרופון** בדפדפון/ב־Electron. בלי הרשאה — הכפתור יגיד זאת, ודיבור
  בלחיצה (`Ctrl+Shift+Space`) והצ'אט בכתב ממשיכים לעבוד.
- **זיהוי הדיבור הוא אוצר פקודות סגור + מילת השכמה**, לא הכתבה חופשית. הכתבה חופשית דורשת מודל
  אקוסטי ענק ומודל שפה — שום דבר כזה לא ניתן לבנות מאפס על המחשב הזה, ואנחנו לא מעמידים פנים.
- **⚠️ הבנק המסופק מזהה את הקול שלנו, לא כל קול. זו המגבלה החשובה ביותר כאן.**
  התבניות מסונתזות מה־TTS שלנו, והדיוק שפורסם קודם (50/50) נמדד כך שה־TTS שלנו הזין את ה־STT שלנו —
  כלומר המערכת נבחנה מול עצמה. מדידה ישירה של אותה מילה בקול אחר:

  | קלט | ציון השכמה |
  |---|---|
  | הקול שלנו (concat) | **1.000** |
  | אותו משפט בגוון קול אחר | **0.000** |
  | גובה צליל ±6/±7 חצאי טון (לא נראה בבנק) | **1.000** אחרי הרחבת הגיוון |
  | קצב 0.92 / 1.08 | 1.000 / 1.000 |
  | עוצמה: 1% עד 100% | 1.000 (נורמליזציית CMVN) |
  | רעש 12dB / 6dB | 1.000 / 0.000 |
  | שקט, רעש, פקודות אחרות, משפט מחוץ לאוצר | 0.000–0.072 (אפס זיהויי שווא) |

  המשמעות המעשית: **קול אנושי אמיתי לא יפעיל את מילת ההשכמה מהבנק המסופק.** לכן שני דברים תוקנן:
  (1) המיקרופון נפתח *כבר מקשיב* — מילת ההשכמה היא נתיב בונוס, לא שער;
  (2) **אימון קול חובה לדיבור אמיתי** — ב־HUD: `🎯 אימון קול`, או בשורת הפקודה
  `python tools/enroll_voice.py --record`. מדידה הדרגתית של קול זר (ביטחון בזיהוי, על ביטויים *שלא* נשמרו):

  | דגימות שלך בבנק | פקודה שאומנה | פקודה שלא אומנה | משפט מחוץ לאוצר |
  |---|---|---|---|
  | 0 | 0.03–0.07 ✗ | 0.03 ✗ | 0.01 ✗ |
  | 1 | **0.92–1.00 ✓** | 0.19 ✗ | 0.01 ✗ |
  | 4 | **0.92–1.00 ✓** | 0.03 ✗ | 0.26 ✗ |

  שתי מסקנות, ושתיהן מוטמעות במוצר: **דגימה בודדת יכולה "למשוך" גם משפטים אחרים** אל התווית שלה
  (הגוון הקולי שולט בתוכן ב־MFCC+DTW), ולכן השרת מחזיר אזהרה כל עוד אומנו פחות מ־3 משפטים
  וה־HUD מציג אותה; ואחרי אימון של כמה משפטים האבחנה חדה — כל פקודה שאומנה מזוהה ב־0.92–1.00,
  ושום דבר אחר לא עובר את הסף. מה שלא אומן עדיין חוזר כ"לא שמעתי" — ואז הכפתור
  `🎯 למד אותי והפעל` מאמן את אותה הקלטה ומריץ אותה מיד.
- **אימון הקול נשמר בבנק התבניות** (`voice/stt/bank`, מחוץ ל־Git). להעברה למיקום אישי:
  `JARVIS_STT_BANK=~/jarvis-voice`.
- **קצב הדיבור היה איטי פי שלושה — נמצא ותוקן.** הקלטות הפונמות הן מילים מבודדות, ולכן כל יחידה
  שמרה על האורך "המקריא" שלה: 148 פונמות יצרו 18.0 שניות קול (122ms לפונמה לעומת ~65ms בדיבור רציף),
  ותשובה של 91 תווים נמשכה **21.5 שניות** — שבהן המיקרופון מושתק והשיחה תקועה. התיקון בשלוש שכבות:
  קיפול "אוויר מת" *בתוך* יחידה (381 יחידות החזיקו 51.9s קלט עבור 44.4s צליל, והגרועה שבהן 0.77s
  עם 0.30s צליל), תקרת משך לכל פונמה (0.17s, ו־0.24s לפונמה מודגשת; דחיסת WSOLA שומרת גובה),
  ושומר קצב כולל לכ־9 תווים/שנייה. התוצאה **10.0 שניות**, ותשובות קצרות ~12 תווים/שנייה.
  המחיר, בכנות: RTF עלה מ־0.11–0.21 ל־0.26–0.56 (עדיין מהיר מזמן אמת), וביטחון הזיהוי הממוצע
  של הבנק ירד מ־0.98 ל־0.93 — שניהם נמדדים בסוויטה.
- **הליבה העצבית קטנה (6.2M פרמטרים)** ולכן היא טובה בניסוח וניתוב, לא בידע כללי פתוח.
  הידע העובדתי מגיע ממאגר הידע ומהכלים, וזו החלטה תכנונית מודעת (מניעת הזיות).
- **3 פונמות (CH, J, o)** חסרות בבנק הקול כי 4 הקלטות גולמיות חסרות; מנוע הפורמנטים מכסה אותן.
  להשלמה: להקליט את `hv_a/hv_e/hv_i/hv_o` ולהריץ `python tools/build_voicebank.py`.
- **סביבת הפיתוח כאן היא Linux ללא מסך**, ולכן קליפת ה־Electron נבדקה סטטית;
  ההרצה האמיתית היא על Windows (היעד שנבחר). השרת וה־HUD נבדקו אנד־טו־אנד דרך HTTP/WS.
- פקודות מערכת שדורשות חלון גרפי (צילום מסך, מקשי מדיה, קליפבורד) מדווחות בכנות שהרכיב
  חסר בסביבה ללא שולחן עבודה, ועובדות על Windows עם `pyperclip`, `mss`, `pyautogui`, `pynput`.
- **בדיקת החיוּת מזהה מסך, לא תצלום מודפס.** מדד המוארה מפריד תצוגה מפנים
  אמיתיות בפער רחב (32.4–105.0 מול 3.83–7.45, סף 12.0 בתוך הפער), אבל דף מוחזק
  מול העדשה לא נתפס. מדד רביעי שנועד לכך נבנה, נמדד, ונמחק — הוא נתן לפנים
  אמיתיות 0.146–0.740 ולדף 0.724, כלומר דלק על פנים אמיתיות ולא הפריד כלום. גם
  רשת פיקסלים עדינה (8px) חומקת, כי התדר שלה נופל מחוץ לטבעת שנמדדת. מה שכן
  מובטח: חיוּת לעולם לא *מעלה* הרשאה, רק חוסמת — פריים חשוד נכפה ל‑`SAFE`.
- **הבעלים עדיין לא רשום.** הגלריה מגיעה ריקה בכוונה: אין כאן מצלמה ואין תצלום,
  והרשמת פנים סינתטיים בשם `OSCAR` הייתה תופסת את מקום הבעלים ולא תואמת אף אחד —
  גרוע מריק. ההרשמה הראשונה שלך תופסת את המקום, מה‑HUD או מ‑
  `python tools/enroll_face.py --image your_face.png`.

---

## מבנה הקוד

```
brain/          tokenizer.py (BPE) · model.py (Transformer+RoPE) · inference.py (NeuralCore)
                reasoning.py (לולאת חשיבה + מאמת) · intent.py (ניתוב) · math_engine.py
                knowledge.py (+knowledge_base.yaml) · memory.py (SQLite) · voicebank/
voice/          dsp.py (MFCC, VAD, F0, DTW, pitch/time) · tts/ (g2p, formant, concat, engine)
                stt/ (זיהוי פקודות + בנק תבניות + WakeListener)
vision/         faces.py (גילוי עור ב־YCbCr · יישור לפי עיניים · eigenfaces · FaceStore
                + FaceGate = הרשאה קשורת נוכחות) — מאפס, בלי OpenCV
skills/         registry.py + 8 מודולים (fs, system, apps, media, screen, scheduler, math, code)
security/       permissions.py (חומת הרשאות + ביקורת + מתג חירום)
agents/         jarvis.py (מנצח) · hephaestus.py (מתכנת)
core/           bus.py (אפיק אירועים = שכבת התצפיתיות) · config.py · server.py (aiohttp,
                כולל /api/faces*) · 
                voice_session.py (שיחה חופשית: VAD + השכמה + תורות + השתקה עצמית)
ui/             index.html · assets/{style.css,app.js,hud.js,audio.js}
electron/       main.js (חלון, tray, hotkeys, השגחה על המוח) · preload.js · assets/icon.png
tools/          forge_corpus · build_tokenizer · train · build_voicebank · build_stt_templates
                enroll_voice · probe_brain
tests/          10 מודולי בדיקה + run_all.py (מאלץ את הילדים ל־UTF-8)
models/         jarvis_nano.pt (משקולות מאומנות, 24.8MB) · tokenizer.json
```

## בניית הכול מאפס (אם רוצים לוודא שאין כאן שום דבר מוכן)

```bash
python tools/forge_corpus.py --size large     # קורפוס עברי+אנגלי+קוד עם סדר ReAct
python tools/build_tokenizer.py               # BPE דו-לשוני משלנו
python tools/train.py --size nano             # אימון הליבה (CPU)
python tools/build_voicebank.py               # חיתוך הקלטות הקול ליחידות
python tools/build_stt_templates.py           # בנק תבניות הזיהוי + כיול עצמי
python tools/probe_brain.py                   # שיחת בדיקה מול המוח
```

## אבטחת הפרטיות

השרת נקשר ל־`127.0.0.1` בלבד (`--preview` קיים רק לסביבת הפיתוח בסנדבוקס).
ה־renderer של Electron רץ עם `contextIsolation`, בלי `nodeIntegration`, ועם CSP הדוק.
הקוד ש־HEPHAESTUS מריץ נכנס לתהליך נפרד עם `-I -B`, סביבה מבודדת, ופסק זמן מחייב.
שום בקשה לא יוצאת מהמכונה. יומן ביקורת מלא נכתב ל־`logs/audit.jsonl`.

---

## מצב הפרויקט

ראה **[PLAN.md](docs/PLAN.md)** — שלבי P0→P11; **P0–P9 הושלמו ונבדקו**, P10 חלקי
(סקריפטי התקנה + הגדרת electron-builder קיימים; אין עדיין exe ארוז), P11 ממתין.

---

*נבנה מאפס. שום דבר כאן לא הורד מוכן.*
