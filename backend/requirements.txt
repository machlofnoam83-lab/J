# ============================================
# Adiel Junior - Requirements v2.0 (UPGRADED 2026)
# Python 3.10+ (מומלץ 3.12 / 3.13)
# FastAPI + כל המערכות - גרסאות מעודכנות ונבדקות
#
# טיפ: ההתקנה המלאה כבדה (torch וכו'). להתקנה קלה בלבד:
#   pip install fastapi "uvicorn[standard]" pydantic aiohttp websockets python-dotenv
# ============================================

# Backend Core - FastAPI + aiohttp (שניהם - תמיכה מלאה)
fastapi>=0.141.0
uvicorn[standard]>=0.52.0
aiohttp>=3.14.0
aiohttp-cors>=0.8.0
websockets>=15.0.0
pydantic>=2.13.0

# Audio - Hebrew STT/TTS - עם דיבור מהיר
faster-whisper>=1.2.0
vosk>=0.3.45
sounddevice>=0.5.5
soundfile>=0.14.0
edge-tts>=7.2.0

# TTS Playback - קול AI אמיתי לכל שאלה
pygame-ce>=2.5.0
webrtcvad-wheels>=2.0.14
pyttsx3>=2.99
SpeechRecognition>=3.14.0

# Vision - קריאת טקסט מתקדמת
mss>=10.2.0
Pillow>=12.0.0
numpy>=2.2.0
opencv-python>=4.12.0.88
pytesseract>=0.3.13
easyocr>=1.7.2
PyMuPDF>=1.28.0

# System & Computer Control
pygetwindow>=0.0.9
pyautogui>=0.9.54
psutil>=7.0.0
requests>=2.32.0

# 🔓 אפס API keys! המערכת 100% ביתית ומקומית:
#    - המוח = AdielMind (ביתי, מאפס) - לא צריך OpenAI/Ollama/שום מפתח
#    - הקול = קולות מקוריים + edge-tts חינמי + pyttsx3 אופליין
#    - שליטה בספוטיפיי/מחשב = מקומית בלבד (בלי spotipy/API)
#    מצב אופליין מלא: הגדר ADIEL_OFFLINE=1

# Utils
python-dotenv>=1.2.0

# === SUPER AGENT ===
playwright>=1.55.0
selenium>=4.35.0
beautifulsoup4>=4.14.0
lxml>=6.0.0
pandas>=2.3.0
openpyxl>=3.1.5
# (הוסרו: google-api / spotipy / openai / elevenlabs - המערכת לא צריכה מפתחות!)

# === AI Model - מודל 3B מובנה (אופציונלי - כבד!) ===
scikit-learn>=1.7.0
torch>=2.8.0
transformers>=4.56.0
bitsandbytes>=0.47.0
accelerate>=1.10.0

# === Fast Speech Understanding ===
rapidfuzz>=3.14.0
jellyfish>=1.2.0

# === Training ===
tqdm>=4.67.0
