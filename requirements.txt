# JARVIS — all dependencies are free/open. No API keys. No cloud services.
# Core numerics (required)
numpy>=1.26
scipy>=1.11

# Neural core (required for training; inference can fall back to pure-numpy)
torch>=2.1

# Audio (required for voice engine)
soundfile>=0.12

# Server (required — the HUD reaches the brain over HTTP and WebSocket).
# core/server.py imports aiohttp at module level, so without it `JARVIS.bat`
# cannot start the brain at all. It was missing from this file while
# tools/setup_windows.bat announced it was installing it, which meant a clean
# Windows install produced a JARVIS with no server.
aiohttp>=3.9

# Knowledge base (required — brain/knowledge.py reads its corpus as YAML, at
# module level, so this is not optional either)
pyyaml>=6.0

# Tests (tests/test_server.py and tests/test_voice_session.py connect to the
# running server over a real WebSocket)
websockets>=12.0

# System operations
psutil>=5.9

# Optional — fast ONNX inference runtime for the trained model
onnxruntime>=1.16

# Optional — JIT acceleration for DSP paths
numba>=0.59

# --- Windows-only (installed automatically by tools/setup_windows.bat) ---
# pywin32        -> Win32 API (windows, input, clipboard)
# mss            -> screenshots
# pyautogui      -> mouse/keyboard automation
# pyperclip      -> clipboard
# sounddevice    -> WASAPI audio I/O
# comtypes       -> Windows SAPI, used as a last-resort output device
